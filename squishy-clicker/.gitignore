# Built place/model files (generated from source, don't commit)
*.rbxl
*.rbxlx
*.rbxm
*.rbxmx

# EXCEPT the real place file: since the world went BAKED, the place holds
# hand-imported models and the hand-edited map — irreplaceable, not
# generated. Save it here via File → Save to File As in Studio.
!place/*.rbxl
!place/*.rbxlx

# Rojo build lock
*.lock

# Editor
.vscode/
.DS_Store

# Rokit-installed tool binaries (rokit.toml is the source of truth)
# (Rokit stores tools globally, not in-project, so nothing to ignore here by default)
